-- Migration already applied to both Neon and local databases via prisma db push
-- This migration file exists to maintain migration history consistency
-- All changes are already in the databases:
-- - Payment, Refund, LegalDocument, Destination tables
-- - User payment fields (hasPaid, paymentDate, subscriptionTier, etc.)
-- - All other schema changes from schema.prisma

